---
interface Props {
  title: string;
  description?: string;
  lang?: string;
}

const { title, description = 'Benjamin Reder - Personal Website', lang = 'en' } = Astro.props;
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <meta name="author" content="Benjamin Reder" />
    <meta name="theme-color" content="#2a8a8a" />

    <!-- Open Graph / Social Media -->
    <meta property="og:type" content="website" />
    <meta property="og:title" content={`${title} | Benjamin Reder`} />
    <meta property="og:description" content={description} />
    <meta property="og:image" content="/images/hero.webp" />
    <meta property="og:url" content="https://benjaminreder.de" />
    <meta name="twitter:card" content="summary_large_image" />

    <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png" />
    <link rel="icon" href="/favicon.ico" sizes="32x32" />
    <link rel="apple-touch-icon" sizes="180x180" href="/apple-touch-icon.png" />
    <link rel="manifest" href="/site.webmanifest" />
    <link rel="preload" as="image" href="/images/hero.webp" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&family=Playfair+Display:wght@400;500;600;700&display=swap" rel="stylesheet" />
    <title>{title} | Benjamin Reder</title>

    <!-- Schema.org JSON-LD -->
    <script type="application/ld+json">
      {
        "@context": "https://schema.org",
        "@type": "Person",
        "name": "Benjamin Reder",
        "url": "https://benjaminreder.de",
        "image": "https://benjaminreder.de/images/profilbild.webp",
        "jobTitle": "Sales Lead & Strategic Partner Manager Digital Solutions",
        "worksFor": {
          "@type": "Organization",
          "name": "Robert Bosch Power Tools"
        },
        "sameAs": [
          "https://www.linkedin.com/in/benjamin-reder",
          "https://github.com/benjaminreder",
          "https://x.com/benjo1910",
          "https://www.instagram.com/ben.rdr/"
        ],
        "address": {
          "@type": "PostalAddress",
          "addressLocality": "Stuttgart",
          "addressCountry": "DE"
        }
      }
    </script>
  </head>
  <body class="bg-background text-foreground antialiased">
    <!-- Navigation -->
    <nav id="main-nav" class="fixed top-0 left-0 right-0 z-50 transition-all duration-300 nav-visible">
      <div class="container mx-auto px-6">
        <div class="flex items-center justify-between h-16">
          <a href="/" class="font-display text-xl font-semibold text-white hover:text-white/80 transition-colors nav-link">
            Benjamin Reder
          </a>

          <!-- Desktop Navigation -->
          <div class="hidden md:flex items-center gap-8">
            <a href="/#about" class="text-sm font-medium text-white/80 hover:text-white transition-colors nav-link">About me</a>
            <a href="/#projects" class="text-sm font-medium text-white/80 hover:text-white transition-colors nav-link">Projects</a>
            <a href="/#books" class="text-sm font-medium text-white/80 hover:text-white transition-colors nav-link">Books</a>
            <a href="/#quotes" class="text-sm font-medium text-white/80 hover:text-white transition-colors nav-link">Quotes</a>
            <a href="/#timeline" class="text-sm font-medium text-white/80 hover:text-white transition-colors nav-link">My Journey</a>
          </div>

          <!-- Mobile Menu Button -->
          <button id="mobile-menu-btn" class="md:hidden p-2 text-white nav-link" aria-label="Toggle menu">
            <svg id="menu-icon" class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
            </svg>
            <svg id="close-icon" class="w-6 h-6 hidden" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M6 18L18 6M6 6l12 12" />
            </svg>
          </button>
        </div>

        <!-- Mobile Navigation -->
        <div id="mobile-menu" class="hidden md:hidden py-4 border-t border-white/20 animate-fade-in bg-black/80 backdrop-blur-md -mx-6 px-6">
          <a href="/#about" class="block py-3 text-white/80 hover:text-white transition-colors mobile-nav-link">About me</a>
          <a href="/#projects" class="block py-3 text-white/80 hover:text-white transition-colors mobile-nav-link">Projects</a>
          <a href="/#books" class="block py-3 text-white/80 hover:text-white transition-colors mobile-nav-link">Books</a>
          <a href="/#quotes" class="block py-3 text-white/80 hover:text-white transition-colors mobile-nav-link">Quotes</a>
          <a href="/#timeline" class="block py-3 text-white/80 hover:text-white transition-colors mobile-nav-link">My Journey</a>
        </div>
      </div>
    </nav>

    <main>
      <slot />
    </main>

    <!-- Footer -->
    <footer class="py-12 bg-background border-t border-border">
      <div class="container mx-auto px-6">
        <div class="flex flex-col md:flex-row items-center justify-between gap-6">
          <div class="text-center md:text-left">
            <p class="font-display text-lg font-semibold text-foreground mb-1">
              Benjamin Reder
            </p>
            <p class="text-sm text-muted-foreground">
              © {new Date().getFullYear()} · All rights reserved
            </p>
          </div>

          <!-- Legal Links -->
          <div class="flex items-center gap-6">
            <a href="/impressum" class="text-sm text-muted-foreground hover:text-primary transition-colors">Impressum</a>
            <a href="/datenschutz" class="text-sm text-muted-foreground hover:text-primary transition-colors">Datenschutz</a>
          </div>

          <!-- Social Links -->
          <div class="flex items-center gap-4">
            <a href="https://www.linkedin.com/in/benjamin-reder" target="_blank" rel="noopener noreferrer" class="text-muted-foreground hover:text-primary transition-colors" aria-label="LinkedIn">
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M19 0h-14c-2.761 0-5 2.239-5 5v14c0 2.761 2.239 5 5 5h14c2.762 0 5-2.239 5-5v-14c0-2.761-2.238-5-5-5zm-11 19h-3v-11h3v11zm-1.5-12.268c-.966 0-1.75-.79-1.75-1.764s.784-1.764 1.75-1.764 1.75.79 1.75 1.764-.783 1.764-1.75 1.764zm13.5 12.268h-3v-5.604c0-3.368-4-3.113-4 0v5.604h-3v-11h3v1.765c1.396-2.586 7-2.777 7 2.476v6.759z"/></svg>
            </a>
            <a href="https://x.com/benjo1910" target="_blank" rel="noopener noreferrer" class="text-muted-foreground hover:text-primary transition-colors" aria-label="X">
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M18.244 2.25h3.308l-7.227 8.26 8.502 11.24H16.17l-5.214-6.817L4.99 21.75H1.68l7.73-8.835L1.254 2.25H8.08l4.713 6.231zm-1.161 17.52h1.833L7.084 4.126H5.117z"/></svg>
            </a>
            <a href="https://www.instagram.com/ben.rdr/" target="_blank" rel="noopener noreferrer" class="text-muted-foreground hover:text-primary transition-colors" aria-label="Instagram">
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M12 2.163c3.204 0 3.584.012 4.85.07 3.252.148 4.771 1.691 4.919 4.919.058 1.265.069 1.645.069 4.849 0 3.205-.012 3.584-.069 4.849-.149 3.225-1.664 4.771-4.919 4.919-1.266.058-1.644.07-4.85.07-3.204 0-3.584-.012-4.849-.07-3.26-.149-4.771-1.699-4.919-4.92-.058-1.265-.07-1.644-.07-4.849 0-3.204.013-3.583.07-4.849.149-3.227 1.664-4.771 4.919-4.919 1.266-.057 1.645-.069 4.849-.069zm0-2.163c-3.259 0-3.667.014-4.947.072-4.358.2-6.78 2.618-6.98 6.98-.059 1.281-.073 1.689-.073 4.948 0 3.259.014 3.668.072 4.948.2 4.358 2.618 6.78 6.98 6.98 1.281.058 1.689.072 4.948.072 3.259 0 3.668-.014 4.948-.072 4.354-.2 6.782-2.618 6.979-6.98.059-1.28.073-1.689.073-4.948 0-3.259-.014-3.667-.072-4.947-.196-4.354-2.617-6.78-6.979-6.98-1.281-.059-1.69-.073-4.949-.073zm0 5.838c-3.403 0-6.162 2.759-6.162 6.162s2.759 6.163 6.162 6.163 6.162-2.759 6.162-6.163c0-3.403-2.759-6.162-6.162-6.162zm0 10.162c-2.209 0-4-1.79-4-4 0-2.209 1.791-4 4-4s4 1.791 4 4c0 2.21-1.791 4-4 4zm6.406-11.845c-.796 0-1.441.645-1.441 1.44s.645 1.44 1.441 1.44c.795 0 1.439-.645 1.439-1.44s-.644-1.44-1.439-1.44z"/></svg>
            </a>
            <a href="https://github.com/benjaminreder" target="_blank" rel="noopener noreferrer" class="text-muted-foreground hover:text-primary transition-colors" aria-label="GitHub">
              <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24"><path d="M12 0c-6.626 0-12 5.373-12 12 0 5.302 3.438 9.8 8.207 11.387.599.111.793-.261.793-.577v-2.234c-3.338.726-4.033-1.416-4.033-1.416-.546-1.387-1.333-1.756-1.333-1.756-1.089-.745.083-.729.083-.729 1.205.084 1.839 1.237 1.839 1.237 1.07 1.834 2.807 1.304 3.492.997.107-.775.418-1.305.762-1.604-2.665-.305-5.467-1.334-5.467-5.931 0-1.311.469-2.381 1.236-3.221-.124-.303-.535-1.524.117-3.176 0 0 1.008-.322 3.301 1.23.957-.266 1.983-.399 3.003-.404 1.02.005 2.047.138 3.006.404 2.291-1.552 3.297-1.23 3.297-1.23.653 1.653.242 2.874.118 3.176.77.84 1.235 1.911 1.235 3.221 0 4.609-2.807 5.624-5.479 5.921.43.372.823 1.102.823 2.222v3.293c0 .319.192.694.801.576 4.765-1.589 8.199-6.086 8.199-11.386 0-6.627-5.373-12-12-12z"/></svg>
            </a>
          </div>
        </div>
      </div>
    </footer>

    <style is:global>
      :root {
        /* Warm, sophisticated palette - cream/warm white base */
        --background: 40 30% 98%;
        --foreground: 220 20% 15%;

        --card: 40 25% 96%;
        --card-foreground: 220 20% 15%;

        /* Deep teal as primary - sophisticated and modern */
        --primary: 185 60% 35%;
        --primary-foreground: 40 30% 98%;

        --secondary: 40 20% 92%;
        --secondary-foreground: 220 20% 15%;

        --muted: 40 15% 90%;
        --muted-foreground: 220 10% 45%;

        /* Warm amber accent */
        --accent: 35 90% 55%;
        --accent-foreground: 220 20% 10%;

        --border: 40 20% 88%;

        --shadow-soft: 0 4px 20px hsl(220 20% 15% / 0.08);
        --shadow-card: 0 8px 30px hsl(220 20% 15% / 0.1);
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      html {
        scroll-behavior: smooth;
      }

      /* Focus styles for accessibility */
      *:focus-visible {
        outline: 2px solid hsl(var(--primary));
        outline-offset: 2px;
      }

      body {
        font-family: 'Inter', system-ui, sans-serif;
        background-color: hsl(var(--background));
        color: hsl(var(--foreground));
      }

      .font-display {
        font-family: 'Playfair Display', Georgia, serif;
      }

      .container {
        max-width: 1200px;
        margin: 0 auto;
      }

      .bg-background { background-color: hsl(var(--background)); }
      .bg-card { background-color: hsl(var(--card)); }
      .text-foreground { color: hsl(var(--foreground)); }
      .text-muted-foreground { color: hsl(var(--muted-foreground)); }
      .text-primary { color: hsl(var(--primary)); }
      .bg-primary { background-color: hsl(var(--primary)); }
      .text-primary-foreground { color: hsl(var(--primary-foreground)); }
      .bg-secondary { background-color: hsl(var(--secondary)); }
      .text-secondary-foreground { color: hsl(var(--secondary-foreground)); }
      .border-border { border-color: hsl(var(--border)); }

      /* Text gradient - teal to amber */
      .text-gradient {
        background: linear-gradient(135deg, hsl(185 60% 35%), hsl(35 90% 55%));
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        background-clip: text;
      }

      /* Animations */
      @keyframes fadeIn {
        from { opacity: 0; }
        to { opacity: 1; }
      }

      @keyframes fadeInUp {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      @keyframes scaleIn {
        from {
          opacity: 0;
          transform: scale(0.95);
        }
        to {
          opacity: 1;
          transform: scale(1);
        }
      }

      .animate-fade-in {
        animation: fadeIn 0.6s ease-out forwards;
      }

      .animate-fade-in-up {
        animation: fadeInUp 0.8s ease-out forwards;
      }

      .animate-scale-in {
        animation: scaleIn 0.5s ease-out forwards;
      }

      /* Card hover effect */
      .card-hover {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
      }

      .card-hover:hover {
        transform: translateY(-4px);
        box-shadow: var(--shadow-card);
      }

      /* Shadow utilities */
      .shadow-soft {
        box-shadow: var(--shadow-soft);
      }

      .shadow-card {
        box-shadow: var(--shadow-card);
      }

      /* Glass card effect */
      .glass-card {
        background: rgba(255, 255, 255, 0.03);
        backdrop-filter: blur(10px);
        border: 1px solid rgba(255, 255, 255, 0.1);
        border-radius: 1rem;
      }

      /* Hover lift effect */
      .hover-lift {
        transition: transform 0.3s ease, box-shadow 0.3s ease;
      }

      .hover-lift:hover {
        transform: translateY(-4px);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.2);
      }

      /* Animation delays */
      .animation-delay-100 { animation-delay: 0.1s; }
      .animation-delay-200 { animation-delay: 0.2s; }
      .animation-delay-300 { animation-delay: 0.3s; }
      .animation-delay-400 { animation-delay: 0.4s; }


      /* Nav hide/show animation */
      #main-nav {
        transform: translateY(0);
        opacity: 1;
      }

      #main-nav.nav-hidden {
        transform: translateY(-100%);
        opacity: 0;
        pointer-events: none;
      }

      #main-nav.nav-visible {
        transform: translateY(0);
        opacity: 1;
        pointer-events: auto;
      }
    </style>

    <script>
      // Mobile menu toggle
      const menuBtn = document.getElementById('mobile-menu-btn');
      const mobileMenu = document.getElementById('mobile-menu');
      const menuIcon = document.getElementById('menu-icon');
      const closeIcon = document.getElementById('close-icon');

      menuBtn?.addEventListener('click', () => {
        mobileMenu?.classList.toggle('hidden');
        menuIcon?.classList.toggle('hidden');
        closeIcon?.classList.toggle('hidden');
      });

      // Close mobile menu when clicking a link
      mobileMenu?.querySelectorAll('a').forEach(link => {
        link.addEventListener('click', () => {
          mobileMenu?.classList.add('hidden');
          menuIcon?.classList.remove('hidden');
          closeIcon?.classList.add('hidden');
        });
      });

      // Navbar scroll effect
      const nav = document.getElementById('main-nav');
      const navLinks = document.querySelectorAll('.nav-link');
      const mobileNavLinks = document.querySelectorAll('.mobile-nav-link');
      const isHomePage = window.location.pathname === '/' || window.location.pathname === '';

      function updateNavStyle() {
        // On subpages, always use solid background with dark text
        if (!isHomePage) {
          nav?.classList.add('bg-background/95', 'backdrop-blur-md', 'border-b', 'border-border/50', 'scrolled');
          navLinks.forEach(link => {
            link.classList.remove('text-white', 'text-white/80');
            link.classList.add('text-foreground', 'text-muted-foreground');
          });
          mobileNavLinks.forEach(link => {
            link.classList.remove('text-white', 'text-white/80');
            link.classList.add('text-foreground', 'text-muted-foreground');
          });
          mobileMenu?.classList.remove('border-white/20', 'bg-black/80');
          mobileMenu?.classList.add('border-border/50', 'bg-background/95');
          return;
        }

        // On homepage, switch based on scroll position
        if (window.scrollY > 100) {
          nav?.classList.add('bg-background/95', 'backdrop-blur-md', 'border-b', 'border-border/50', 'scrolled');
          navLinks.forEach(link => {
            link.classList.remove('text-white', 'text-white/80');
            link.classList.add('text-foreground', 'text-muted-foreground');
          });
          mobileNavLinks.forEach(link => {
            link.classList.remove('text-white', 'text-white/80');
            link.classList.add('text-foreground', 'text-muted-foreground');
          });
          mobileMenu?.classList.remove('border-white/20', 'bg-black/80');
          mobileMenu?.classList.add('border-border/50', 'bg-background/95');
        } else {
          nav?.classList.remove('bg-background/95', 'backdrop-blur-md', 'border-b', 'border-border/50', 'scrolled');
          navLinks.forEach(link => {
            link.classList.remove('text-foreground', 'text-muted-foreground');
            link.classList.add('text-white', 'text-white/80');
          });
          mobileNavLinks.forEach(link => {
            link.classList.remove('text-foreground', 'text-muted-foreground');
            link.classList.add('text-white', 'text-white/80');
          });
          mobileMenu?.classList.remove('border-border/50', 'bg-background/95');
          mobileMenu?.classList.add('border-white/20', 'bg-black/80');
        }
      }

      window.addEventListener('scroll', updateNavStyle);
      updateNavStyle();

      // Hide nav after About section using Intersection Observer
      const aboutSection = document.getElementById('about');

      if (aboutSection && nav) {
        const observer = new IntersectionObserver(
          (entries) => {
            entries.forEach((entry) => {
              if (entry.isIntersecting) {
                // About section is visible - show nav
                nav.classList.remove('nav-hidden');
                nav.classList.add('nav-visible');
              } else {
                // Check if we scrolled past the about section (not above it)
                const rect = aboutSection.getBoundingClientRect();
                if (rect.bottom < 0) {
                  // We are below the about section - hide nav
                  nav.classList.remove('nav-visible');
                  nav.classList.add('nav-hidden');
                } else {
                  // We are above the about section (hero area) - show nav
                  nav.classList.remove('nav-hidden');
                  nav.classList.add('nav-visible');
                }
              }
            });
          },
          {
            root: null,
            rootMargin: '0px',
            threshold: 0
          }
        );

        observer.observe(aboutSection);
      }
    </script>
  </body>
</html>
